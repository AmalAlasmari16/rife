/// Barrel file for app-wide Riverpod providers.
///
/// As we add features in the next steps (auth, subscription, repositories),
/// each provider gets its own file alongside this one and is re-exported here
/// so screens have a single import.
library;
